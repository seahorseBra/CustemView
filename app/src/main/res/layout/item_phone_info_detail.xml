<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="44dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp">
    <TextView
        android:id="@+id/item_phone_info_detail_name"
        android:gravity="center"
        android:textColor="#3e3e3e"
        tools:text="信息"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/item_phone_info_detail_info"
        android:gravity="right|center"
        android:textColor="#aeaeae"
        tools:text="信息"
        android:layout_toRightOf="@id/item_phone_info_detail_name"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="10dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:ems="10"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />
    <View android:layout_alignParentBottom="true"
        style="@style/device_line"></View>
</RelativeLayout>